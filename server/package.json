{
  "name": "voicepad-transcription-server",
  "private": true,
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "start": "node index.mjs",
    "test": "NODE_ENV=test REQUIRE_AUTH=true ALLOWED_ORIGINS=https://app.voicepad.test node --test test/server.test.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
