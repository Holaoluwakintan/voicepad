{
     "buildCommand": "corepack enable && corepack prepare pnpm@10.4.1 --activate && cd web && pnpm install --frozen-lockfile && pnpm run build",,
  "outputDirectory": "web/dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
